export interface Contact {
   id: number,
   name: string,
   lastName: string,
   email: string,
   phone: number
}
//Esse módulo será usado para compartilhamento
//Foi criado um contrutor já com os atributos, para ele criar automaticamente
// o ? significa que eles são opcionais na chamada do construtor


